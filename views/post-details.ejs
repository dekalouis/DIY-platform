<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DIY Platform - <%= post.title %></title>
    <style>
      .description {
        white-space: pre-line;
        margin: 15px 0px;
      }
      .button {
        text-decoration: none;
        padding: 5px 10px;
        border-radius: 5px;
        color: white;
        margin-bottom: 20px;
        background-color: #2b6cb0;
      }
      .button:hover {
        opacity: 0.8;
      }

      img {
        max-width: 400px;
      }
    </style>
  </head>
  <body>
    <div>
      <h1><%= post.title %></h1>
      <%- include('partials/navbar') %>
      <div>
        <img src="<%= post.imageUrl %>" alt="<%= post.title %>" />
      </div>
      <div>
        <p>
          <strong>Estimated Time:</strong> <%= post.estimatedTime %> Minutes
        </p>
        <p><strong>Difficulty:</strong> <%= post.difficulty %></p>
        <p><strong>Author:</strong> <%= post.authorName %></p>
        <p><strong>Created Date:</strong> <%= post.createdDate %></p>
        <p><strong>Total Vote:</strong> <%= post.totalVote %></p>
        <a class="button" href="/posts/<%= post.id %>/vote">Vote</a>
      </div>
      <div class="description"><%= post.description %></div>
      <a href="/posts" class="button">Back</a>
    </div>
  </body>
</html>
